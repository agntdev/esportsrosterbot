[
  { "name": "Публичные таблицы показывают пустое состояние", "steps": [{ "send": { "callback": "teams:show" }, "expect": [{ "method": "editMessageText", "payload": { "text": "Команд пока нет — зарегистрируйте первую заявку." } }] }, { "send": { "callback": "matches:show" }, "expect": [{ "method": "editMessageText", "payload": { "text": "Турнир ещё не составлен — таблица появится после решения организатора." } }] }] },
  { "name": "Подготовка турнира сообщает, когда состав ещё не опубликован", "steps": [{ "send": { "callback": "tournament:show" }, "expect": [{ "method": "editMessageText", "payload": { "text": "Турнир ещё не составлен — таблица появится после решения организатора." } }] }] }
]
