[
  { "name": "Публичные таблицы показывают пустое состояние", "steps": [{ "send": { "callback": "teams:show" }, "expect": [{ "method": "editMessageText", "payload": { "text": "Команд пока нет — зарегистрируйте первую заявку." } }] }, { "send": { "callback": "matches:show" }, "expect": [{ "method": "editMessageText", "payload": { "text": "Подтверждённых команд пока нет — таблица появится после регистрации." } }] }] }
]
