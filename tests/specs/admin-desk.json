[
  {
    "name": "Organizer controls deny access until owner identity is configured",
    "steps": [
      { "send": { "callback": "admin:desk" }, "expect": [{ "method": "sendMessage", "payload": { "text": "Доступ администратора ещё не настроен." } }] }
    ]
  },
  {
    "name": "Составление турнира доступно только администратору",
    "steps": [
      { "send": { "callback": "admin:tournament:compose" }, "expect": [{ "method": "sendMessage", "payload": { "text": "Доступ администратора ещё не настроен." } }] }
    ]
  }
  ,
  {
    "name": "Назначение расписания закрыто без настроенного доступа администратора",
    "steps": [
      { "send": { "callback": "admin:match:time:m1" }, "expect": [{ "method": "sendMessage", "payload": { "text": "Доступ администратора ещё не настроен." } }] }
    ]
  },
  {
    "name": "Запуск турнира закрыт без настроенного доступа администратора",
    "steps": [
      { "send": { "callback": "admin:tournament:start" }, "expect": [{ "method": "answerCallbackQuery" }, { "method": "sendMessage", "payload": { "text": "Доступ администратора ещё не настроен." } }] }
    ]
  },
  {
    "name": "Подтверждение результата закрыто без настроенного доступа администратора",
    "steps": [
      { "send": { "callback": "admin:match:confirm:t1:m1" }, "expect": [{ "method": "answerCallbackQuery" }, { "method": "sendMessage", "payload": { "text": "Доступ администратора ещё не настроен." } }] }
    ]
  },
  {
    "name": "Прикрепление ссылки закрыто без настроенного доступа администратора",
    "steps": [
      { "send": { "callback": "admin:attach_link" }, "expect": [{ "method": "answerCallbackQuery" }, { "method": "sendMessage", "payload": { "text": "Доступ администратора ещё не настроен." } }] }
    ]
  }
]
